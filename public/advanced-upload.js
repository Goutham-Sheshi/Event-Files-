(()=>{
  const SESSION_KEY='sheshi_vault_session';
  const SUPABASE_URL='https://ikkyziyugrnkolqnrxfo.supabase.co';
  const ANON_KEY='eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imlra3l6aXl1Z3Jua29scW5yeGZvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODY5NTQ4MjUsImV4cCI6MjEwMjUzMDgyNX0.ISewj3DuJNmZrrWqByDwGMk9iys8kXYlTDuYCSYr-j4';
  const advancedRoles=new Set(['advanced','advanced_user','advanced user','teammate','power_user']);
  const products=['sheshi','quanta','catalyx','fr','consultease'];
  const labels={sheshi:'Sheshi',quanta:'Quanta',catalyx:'Catalyx',fr:'FR',consultease:'Consultease'};
  const types=['logo','brochure','video','document','photo','other'];
  const icon='<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3v12M7 8l5-5 5 5M5 21h14"/><path d="M5 14v5a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2v-5"/></svg>';
  function profile(){try{return JSON.parse(localStorage.getItem(SESSION_KEY)||'null')}catch{return null}}
  function isAdvanced(){const p=profile();const r=String(p?.role||'').toLowerCase().trim();return advancedRoles.has(r)}
  function findNav(){const buttons=[...document.querySelectorAll('aside button,aside a')];return buttons.find(x=>/^events$/i.test(x.textContent.trim()))||null}
  function addNav(){if(!isAdvanced()||document.getElementById('advanced-upload-nav'))return;const events=findNav();if(!events)return;const n=document.createElement(events.tagName.toLowerCase()==='a'?'a':'button');n.id='advanced-upload-nav';n.type='button';n.innerHTML=icon+'<span>Upload Files</span>';n.style.cssText=events.style.cssText||'';n.className=events.className;n.setAttribute('aria-label','Upload Files');n.addEventListener('click',e=>{e.preventDefault();openUpload()});events.parentElement?.insertBefore(n,events)}
  function openUpload(){const main=document.querySelector('main');if(!main)return;const previous=main.innerHTML;main.dataset.uploadPrevious=previous;main.style.overflowY='auto';main.innerHTML=`<div style="padding:32px;max-width:980px;width:100%;box-sizing:border-box"><div style="display:flex;align-items:center;justify-content:space-between;gap:16px;margin-bottom:26px"><div><div style="font-size:11px;font-family:monospace;text-transform:uppercase;letter-spacing:.12em;color:var(--ink-45)">Advanced User</div><h1 style="font-size:34px;margin:7px 0 0">Upload Files</h1><p style="font-size:13px;color:var(--ink-45);margin:8px 0 0">Add files to the shared vault with a product, file type, tags and description.</p></div><button id="upload-back" style="border:1px solid var(--line-soft);background:var(--canvas);color:var(--ink);border-radius:9px;padding:10px 14px;cursor:pointer">Back</button></div><div style="border:1px solid var(--line-soft);background:var(--surface);border-radius:18px;padding:24px;max-width:760px"><div style="display:grid;grid-template-columns:1fr 1fr;gap:16px"><label style="font-size:12px;font-weight:600">Related Product<select id="up-product" style="display:block;width:100%;margin-top:8px;padding:13px;border:1px solid var(--line-soft);border-radius:10px;background:var(--canvas);color:var(--ink)">${products.map(p=>`<option value="${p}">${labels[p]}</option>`).join('')}</select></label><label style="font-size:12px;font-weight:600">File Type<select id="up-type" style="display:block;width:100%;margin-top:8px;padding:13px;border:1px solid var(--line-soft);border-radius:10px;background:var(--canvas);color:var(--ink)">${types.map(t=>`<option value="${t}">${t.charAt(0).toUpperCase()+t.slice(1)}</option>`).join('')}</select></label></div><label style="display:block;font-size:12px;font-weight:600;margin-top:16px">Tags<input id="up-tags" placeholder="e.g. branding, event, 2026" style="display:block;box-sizing:border-box;width:100%;margin-top:8px;padding:13px;border:1px solid var(--line-soft);border-radius:10px;background:var(--canvas);color:var(--ink)"/><span style="display:block;margin-top:6px;color:var(--ink-45);font-size:11px;font-weight:400">Separate multiple tags with commas</span></label><label style="display:block;font-size:12px;font-weight:600;margin-top:16px">Description<textarea id="up-description" placeholder="Add a short description for these files" rows="4" style="display:block;box-sizing:border-box;width:100%;margin-top:8px;padding:13px;border:1px solid var(--line-soft);border-radius:10px;background:var(--canvas);color:var(--ink);resize:vertical"></textarea></label><label id="up-drop" style="display:flex;flex-direction:column;align-items:center;justify-content:center;min-height:150px;margin-top:18px;border:2px dashed var(--line-soft);border-radius:12px;cursor:pointer"><strong>Choose files</strong><span id="up-count" style="margin-top:7px;font-size:12px;color:var(--ink-45)">Multiple files supported</span><input id="up-files" type="file" multiple style="display:none"></label><button id="up-submit" style="width:100%;margin-top:16px;padding:14px;border:0;border-radius:10px;background:var(--primary);color:white;font-weight:700;cursor:pointer">Upload Files</button><div id="up-status" style="margin-top:12px;font-size:12px;color:var(--ink-45)"></div></div></div>`;
    document.getElementById('upload-back').onclick=()=>{main.innerHTML=main.dataset.uploadPrevious||'';main.removeAttribute('data-upload-previous')};
    const input=document.getElementById('up-files'),count=document.getElementById('up-count'),drop=document.getElementById('up-drop');
    input.onchange=()=>count.textContent=input.files?.length?`${input.files.length} file${input.files.length===1?'':'s'} selected`:'Multiple files supported';drop.onclick=()=>input.click();
    drop.ondragover=e=>{e.preventDefault();drop.style.borderColor='var(--primary)'};drop.ondragleave=()=>drop.style.borderColor='var(--line-soft)';drop.ondrop=e=>{e.preventDefault();input.files=e.dataTransfer.files;count.textContent=`${input.files.length} file${input.files.length===1?'':'s'} selected`;drop.style.borderColor='var(--line-soft)'};
    document.getElementById('up-submit').onclick=upload;
  }
  async function upload(){const files=[...document.getElementById('up-files').files||[]];const status=document.getElementById('up-status');if(!files.length){status.textContent='Choose at least one file.';return}const p=profile()||{};const product=document.getElementById('up-product').value,type=document.getElementById('up-type').value,tags=document.getElementById('up-tags').value.split(',').map(x=>x.trim()).filter(Boolean),description=document.getElementById('up-description').value.trim();const button=document.getElementById('up-submit');button.disabled=true;button.style.opacity='.65';try{for(let i=0;i<files.length;i++){const f=files[i];status.textContent=`Uploading ${i+1} of ${files.length}: ${f.name}`;const ext=(f.name.split('.').pop()||'FILE').toUpperCase();const path=`${product}/${type}/${new Date().toISOString().slice(0,7)}/${crypto.randomUUID()}-${f.name.toLowerCase().replace(/[^a-z0-9._-]+/g,'-')}`;const auth=ANON_KEY;const put=await fetch(`${SUPABASE_URL}/storage/v1/object/event-assets/${path}`,{method:'POST',headers:{apikey:ANON_KEY,Authorization:`Bearer ${auth}`,'Content-Type':f.type||'application/octet-stream','x-upsert':'false'},body:f});if(!put.ok)throw new Error(await put.text());const row={title:f.name,description:description||null,type,product_id:product,source_url:path,thumbnail:null,storage_path:path,file_format:ext,file_size:formatSize(f.size),tags,featured:false,uploaded_by:p.id||null,uploaded_by_name:p.full_name||null};const ins=await fetch(`${SUPABASE_URL}/rest/v1/vault_resources`,{method:'POST',headers:{apikey:ANON_KEY,Authorization:`Bearer ${auth}`,'Content-Type':'application/json',Prefer:'return=minimal'},body:JSON.stringify(row)});if(!ins.ok){await fetch(`${SUPABASE_URL}/storage/v1/object/event-assets/${path}`,{method:'DELETE',headers:{apikey:ANON_KEY,Authorization:`Bearer ${auth}`});throw new Error(await ins.text())}}status.textContent=`Successfully uploaded ${files.length} file${files.length===1?'':'s'}.`;inputReset()}catch(e){console.error(e);status.textContent='Upload failed. '+(e?.message||'Please try again.')}finally{button.disabled=false;button.style.opacity='1'}}
  function inputReset(){const x=document.getElementById('up-files');if(x){x.value='';document.getElementById('up-count').textContent='Multiple files supported'}}
  function formatSize(b){if(b<1024)return `${b} B`;const u=['KB','MB','GB'];let v=b/1024,i=0;while(v>=1024&&i<u.length-1){v/=1024;i++}return `${v.toFixed(v>=10?0:1)} ${u[i]}`}
  const queue=()=>requestAnimationFrame(addNav);new MutationObserver(queue).observe(document.documentElement,{childList:true,subtree:true});document.readyState==='loading'?document.addEventListener('DOMContentLoaded',queue):queue();
})();
