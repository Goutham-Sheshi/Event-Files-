import { useEffect, useState, type ReactNode } from 'react'
import AdminEvents from './AdminEvents'
import AdminResources from './AdminResources'
import AdminUsers from './AdminUsers'
import { getMyProfile, type VaultProfile } from './authApi'
type IconName='user'|'calendar'|'folder'
function NavIcon({name}:{name:IconName}){const paths:Record<IconName,ReactNode>={user:<><path d="M20 21a8 8 0 0 0-16 0"/><circle cx="12" cy="7" r="4"/></>,calendar:<><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M16 3v4M8 3v4M3 10h18"/></>,folder:<><path d="M3 7a2 2 0 0 1 2-2h5l2 2h7a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/></>};return <svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.9" strokeLinecap="round" strokeLinejoin="round" className="w-4 h-4 shrink-0">{paths[name]}</svg>}
export default function AdminConsole(){const[profile,setProfile]=useState<VaultProfile|null>(null),[tab,setTab]=useState<'users'|'events'|'resources'>('resources');useEffect(()=>{getMyProfile().then(p=>{setProfile(p);if(p?.role==='admin')setTab('users')})},[]);if(!profile)return <div className="p-8 text-sm text-[var(--ink-45)]">Loading access…</div>;const isAdmin=profile.role==='admin';if(!isAdmin)return <div data-admin-console="true" className="flex-1 self-stretch w-full min-w-0 min-h-0 flex"><aside className="w-52 shrink-0 border-r border-[var(--line-soft)] bg-white p-4"><div className="text-[10px] font-mono uppercase tracking-wider text-[var(--ink-45)] px-3 mb-3">Advanced User</div><button onClick={()=>setTab('resources')} className={`w-full inline-flex items-center gap-2 px-3 py-2.5 rounded-lg text-left text-[12px] font-semibold ${tab==='resources'?'bg-[var(--primary)] text-white':'text-[var(--ink-45)] hover:bg-black/5'}`}><NavIcon name="folder"/>Upload Files</button></aside><div className="flex-1 min-w-0">{tab==='resources'&&<AdminResources canDelete={false}/>}</div></div>;const tabs=[{id:'users' as const,label:'User Info & Access',icon:'user' as IconName},{id:'events' as const,label:'Events',icon:'calendar' as IconName},{id:'resources' as const,label:'Related Products & Files',icon:'folder' as IconName}];return <div data-admin-console="true" className="flex-1 self-stretch w-full min-w-0 min-h-0 flex flex-col"><div className="px-8 pt-5 border-b border-[var(--line-soft)] bg-white sticky top-0 z-10"><div className="flex items-center gap-1">{tabs.map(i=><button key={i.id} onClick={()=>setTab(i.id)} className={`inline-flex items-center gap-2 px-4 py-2.5 text-[12px] font-semibold border-b-2 ${tab===i.id?'border-[var(--primary)] text-[var(--primary)]':'border-transparent text-[var(--ink-45)]'}`}><NavIcon name={i.icon}/>{i.label}</button>)}</div></div>{tab==='users'?<AdminUsers/>:tab==='events'?<AdminEvents onChanged={()=>window.dispatchEvent(new Event('vault-events-changed'))}/>:<AdminResources canDelete/>}</div>}
