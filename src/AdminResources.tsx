import { ChangeEvent, useEffect, useState } from 'react'
import { products } from './data'
import type { ResourceType } from './types'
import { deleteManagedResource, getErrorMessage, getManagedResources, uploadResource, type ManagedResource } from './resourcesApi'
const TYPES:ResourceType[]=['logo','brochure','video','document','other']
export default function AdminResources({canDelete=true}:{canDelete?:boolean}){const[items,setItems]=useState<ManagedResource[]>([]),[files,setFiles]=useState<File[]>([]),[productId,setProductId]=useState('sheshi'),[type,setType]=useState<ResourceType>('document'),[description,setDescription]=useState(''),[tagsInput,setTagsInput]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState(''),[notice,setNotice]=useState('');const load=async()=>{try{setItems(await getManagedResources())}catch(e){setError(getErrorMessage(e,'Failed to load files'))}};useEffect(()=>{load()},[]);const pick=(e:ChangeEvent<HTMLInputElement>)=>setFiles(Array.from(e.target.files||[]));const upload=async()=>{if(!files.length)return;setBusy(true);setError('');setNotice('');const count=files.length,tags=tagsInput.split(',').map(t=>t.trim()).filter(Boolean);try{for(const file of files)await uploadResource({title:file.name.replace(/\.[^.]+$/,''),description:description.trim()||null,type,productId,tags},file);setFiles([]);setDescription('');setTagsInput('');setNotice(`${count} file${count===1?'':'s'} uploaded successfully.`);await load()}catch(e){setError(getErrorMessage(e,'Upload failed'))}finally{setBusy(false)}};const remove=async(item:ManagedResource)=>{if(!canDelete||!window.confirm(`Delete "${item.title}"?`))return;setBusy(true);try{await deleteManagedResource(item);await load()}catch(e){setError(getErrorMessage(e,'Delete failed'))}finally{setBusy(false)}};return <div className="px-8 py-6 max-w-[1400px] min-h-full"><div className="mb-6"><h1 className="font-display text-[22px] font-bold text-[var(--ink)]">{canDelete?'Related Products & Files':'Upload Files'}</h1><p className="text-[13px] text-[var(--ink-45)] mt-1">{canDelete?'Manage the shared library and see who uploaded each file.':'Upload files to the shared library. You can view and download files but do not have admin controls.'}</p></div><div className="grid grid-cols-1 xl:grid-cols-[390px_minmax(0,1fr)] gap-6"><div className="bg-white border border-[var(--line-soft)] rounded-2xl p-5 h-fit"><h2 className="font-semibold text-[14px] mb-4">Add files</h2><div className="space-y-3"><label className="block text-[12px] font-medium">Related Product<select value={productId} onChange={e=>setProductId(e.target.value)} className="mt-1.5 w-full px-3 py-2.5 rounded-lg border"><option value="sheshi">Sheshi</option>{products.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label className="block text-[12px] font-medium">File Type<select value={type} onChange={e=>setType(e.target.value as ResourceType)} className="mt-1.5 w-full px-3 py-2.5 rounded-lg border">{TYPES.map(t=><option key={t} value={t}>{t}</option>)}</select></label><label className="block text-[12px] font-medium">Tags<input value={tagsInput} onChange={e=>setTagsInput(e.target.value)} placeholder="e.g. branding, event, 2026" className="mt-1.5 w-full px-3 py-2.5 rounded-lg border"/><span className="block mt-1 text-[10px] text-[var(--ink-45)]">Separate multiple tags with commas</span></label><label className="block text-[12px] font-medium">Description<textarea value={description} onChange={e=>setDescription(e.target.value)} placeholder="Add a short description for these files" rows={3} className="mt-1.5 w-full px-3 py-2.5 rounded-lg border resize-y"/></label><label className="block rounded-xl border-2 border-dashed p-5 text-center cursor-pointer"><input type="file" multiple onChange={pick} className="hidden"/><div className="text-[13px] font-semibold">Choose files</div><div className="text-[11px] text-[var(--ink-45)] mt-1">Multiple files supported</div></label>{files.length>0&&<div className="text-[11px] space-y-1">{files.map(f=><div key={`${f.name}-${f.size}`}>{f.name}</div>)}</div>}{error&&<div className="text-[12px] text-red-600">{error}</div>}{notice&&<div className="text-[12px] text-green-700">{notice}</div>}<button disabled={busy||!files.length} onClick={upload} className="w-full px-4 py-2.5 rounded-lg bg-[var(--primary)] text-white text-[12px] font-semibold disabled:opacity-40">{busy?'Uploading…':`Upload ${files.length||''} File${files.length===1?'':'s'}`}</button></div></div><div className="bg-white border border-[var(--line-soft)] rounded-2xl divide-y"><div className="px-5 py-4 font-semibold text-[14px]">Shared Library</div>{items.length===0?<div className="px-5 py-10 text-[12px] text-[var(--ink-45)]">No files yet.</div>:items.map(item=><div key={item.id} className="px-5 py-3.5 flex items-center gap-3"><div className="min-w-0 flex-1"><div className="text-[12.5px] font-semibold truncate">{item.title}</div><div className="flex items-center gap-2 mt-1 flex-wrap"><span className="text-[11px] text-[var(--ink-45)]">{item.type} · {item.fileSize||'—'}</span>{canDelete&&<span className="inline-flex items-center rounded-full border border-[var(--primary)]/25 bg-[var(--primary)]/10 px-2 py-0.5 text-[10px] font-semibold text-[var(--primary)]">Uploaded by: {item.uploadedByName||'Existing library'}</span>}</div></div><a href={item.sourceUrl} target="_blank" rel="noreferrer" className="text-[11px] font-semibold text-[var(--primary)]">Open</a>{canDelete&&<button disabled={busy} onClick={()=>remove(item)} className="text-[11px] font-semibold text-red-600">Delete</button>}</div>)}</div></div></div>}
